# Pinned loosely: this is a demo, not a product. Torch comes from the CPU index
# (see Makefile) so no CUDA wheels are pulled onto a laptop.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
python-multipart>=0.0.9
PyJWT>=2.9
cryptography>=43
numpy>=1.26
scipy>=1.13
scikit-learn>=1.5
soundfile>=0.12
PyYAML>=6.0
httpx>=0.27
pytest>=8.3
pytest-asyncio>=0.24

# Speech + language models. All run on CPU.
faster-whisper>=1.0.3
sentence-transformers>=3.0
speechbrain>=1.0
silero-vad>=5.1
piper-tts>=1.2          # fallback voice
kokoro-onnx>=0.4        # primary voice, markedly more natural on CPU
onnxruntime>=1.18

# Diagrams for the paper.
matplotlib>=3.9
