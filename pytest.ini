[pytest]
testpaths = backend/tests
pythonpath = backend
markers =
    slow: needs a downloaded speech or embedding model
    e2e: drives a real browser against a real stack on ports 8099 and 5199
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
